{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
<div class="container-fluid vh-100 d-flex justify-content-center align-items-center">
    <div class="row justify-content-center align-items-center w-100">
        <div class="col-12 col-md-8 text-center">
            <h1 class="mb-4">Dashboard Admin</h1>
            
            <a href="{{ path('home_index') }}" class="btn btn-secondary mb-4">
                <i class="fa fa-arrow-left"></i> Retour sur Home
            </a>

            <div class="row justify-content-center">
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">Gestion des Avis</h5>
                            <a href="{{ path('admin_avis') }}" class="btn btn-primary mt-3">
                                <i class="fa fa-comments"></i> Gérer les avis
                            </a>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">Gestion des Utilisateurs</h5>
                            <a href="{{ path('admin_users') }}" class="btn btn-primary mt-3">
                                <i class="fa fa-users"></i> Liste des utilisateurs
                            </a>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">Ajouter un Produit</h5>
                            <a href="{{ path('admin_produit_insert') }}" class="btn btn-primary mt-3">
                                <i class="fa fa-plus"></i> Nouveau Produit
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}